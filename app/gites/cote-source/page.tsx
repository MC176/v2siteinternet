'use client'

import GiteTemplate from '@/components/GiteTemplate'

export default function CoteSourcePage() {
  return (
    <GiteTemplate
      name="Côté Source"
      description="Gîte en Ardèche"
      images={[]} // Will be populated with actual images
    />
  )
}